class pictureInfo:

    def __init__(self, pInfo):
        pId = pInfo.pId         #
        uuid = pInfo.uuid       #나중에
        like = pInfo.like
        friend = pInfo.friend
        pUrl = pInfo.pUrl       #